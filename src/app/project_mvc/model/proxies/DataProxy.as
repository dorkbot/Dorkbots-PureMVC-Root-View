package app.project_mvc.model.proxies{	import app.project_mvc.model.proxy_vos.DataVO;	import app.project_mvc.notifications.notification_objects.StartupNotification;		import org.puremvc.as3.multicore.interfaces.IProxy;	import org.puremvc.as3.multicore.patterns.proxy.Proxy;
		public class DataProxy extends Proxy implements IProxy	{		public static const NAME:String = "DataProxy";				public function DataProxy(startupVO:StartupNotification, aVo:Object = null)		{			if (aVo == null)			{				aVo = new DataVO();			}						super( NAME, aVo );						vo.rootView = startupVO.rootView;			trace("[DataProxy] startupVO.relativePath = " + startupVO.relativePath);			vo.relativePath = startupVO.relativePath;		}				public final function get vo():DataVO		{			return data as DataVO;		}	}}